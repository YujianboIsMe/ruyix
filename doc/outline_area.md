# 大纲区

大纲区分为大纲和GIT
# 大纲
大纲是树状显示的。
做个限制：最多只支持三级大纲。

目前支持的语言列表：
- markdown，编辑器打开的是markdown文件，则显示标题大纲。
- Rust，显示的是语法级别的大纲
- Python,显示的是语法级别的大纲
- Java，显示的是语法级别的大纲（class/interface/enum/record、构造方法、方法、字段）

# GIT
![GIT](git.md)