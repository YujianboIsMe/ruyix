# ISSUE-01
darkhorse-code.exe程序运行一段时间后，就失去响应，此时不得不关闭。
帮我查一下原因

# ISSUE-02
darkhorse-code.exe打开模拟终端后，在终端里无法输入，而且不久后程序就失去响应，此时不得不关闭程序。

# ISSUE-03
我测试了所有终端功能。
git bash是弹出新窗口。
claude点击不起作用。

git bash 弹出新窗口给了我一个灵感。
我觉得每个终端都可以有两种打开方式：
1. 模拟终端
2. 新窗口

在【导航栏-终端资源】里每个条目的右边加个小图标🪟，这是窗户图标，点击这个图标以新窗口打开，使用操作系统的终端程序。
点击终端文字本身就是默认的模拟终端方式。

# ISSUE-03
新窗口打开终端时报错：
```
Windows找不到'\\’文件。请确定拼写是否正确后再试一次，或通过单击[开始」按钮然后
单击“搜索"搜索该项目。
```
此外还有一个问题，我同时打开了node.js和python两个模拟终端。但是切换到node.js标签后，终端内容还是python的终端！
也就是无论怎么切换标签页，模拟终端内容都是最后一个终端！

# ISSUE-04
新窗口打开终端时报错：
系统找不到文件 \Darkhorse\。
拒绝访问。
系统找不到文件 \Darkhorse\。
为什么之前的git bash能成功新窗口打开。现在不行了呢？
