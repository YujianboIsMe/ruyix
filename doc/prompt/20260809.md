# ISSUE-01
ruyix.exe程序运行一段时间后，就失去响应，此时不得不关闭。
帮我查一下原因

# ISSUE-02
ruyix.exe打开模拟终端后，在终端里无法输入，而且不久后程序就失去响应，此时不得不关闭程序。

# ISSUE-03
我测试了所有终端功能。
git bash是弹出新窗口。
claude点击不起作用。

git bash 弹出新窗口给了我一个灵感。
我觉得每个终端都可以有两种打开方式：
1. 模拟终端
2. 新窗口

在【导航栏-终端资源】里每个条目的右边加个小图标🪟，这是窗户图标，点击这个图标以新窗口打开，使用操作系统的终端程序。
点击终端文字本身就是默认的模拟终端方式。

# ISSUE-04
新窗口打开终端时报错：
```
Windows找不到'\\’文件。请确定拼写是否正确后再试一次，或通过单击[开始」按钮然后
单击“搜索"搜索该项目。
```
此外还有一个问题，我同时打开了node.js和python两个模拟终端。但是切换到node.js标签后，终端内容还是python的终端！
也就是无论怎么切换标签页，模拟终端内容都是最后一个终端！

# ISSUE-05
新窗口打开终端时报错：
系统找不到文件 \Darkhorse\。
拒绝访问。
系统找不到文件 \Darkhorse\。
为什么之前的git bash能成功新窗口打开。现在不行了呢？

# ISSUE-06
为什么API返回失败？
[ai]
api_key = "sk-***"
api_url = "https://api.deepseek.com/v1"
model = "deepseek-v4-flash" 
这地址错了吗？

# ISSUE-07
终端启动Claude报以下错误：
启动失败: 启动进程失败: CreateProcessW 
`"C:\\Users\\yujia\\AppData\\Roaming\\npm\\claude\0"` in cwd 
`Some("C:\\Users\\yujia\\RustroverProjects\\ruyix\0")` failed: %1 不是有效的 Win32 应用程序。 
(os error 193)

# ISSUE-08
做个代码优化。
ai.js里面的标准命令里的命令内容，移动到同目录下的command.md中,这个md文件要打包进exe中。
这样源代码就容易维护了。或者你有更好的方案也可以告诉我。

# ISSUE-09
继续做个代码优化。
ui/main.js代码太长了。
把里面的命令部分抽出来，命名为command.js
