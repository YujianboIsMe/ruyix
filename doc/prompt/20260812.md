# ISSUE-01
我打开终端后，发现终端没有横向滚动条。
这是一个不好的体验，因为如果终端输出太宽则看不到了。
请优化【编辑区-终端】功能

# ISSUE-02
文件树没有创建文件夹功能。

# ISSUE-03
@doc/navigator.md
> ### 运行目标
> 运行目标是一个列表。
> 每个条目右边都有两个功能：
> 1. 图标🗑️，点击可以删除运行目标。删除运行目标通过命令系统执行。
> 2. 图标✍️，修改运行目标

```

# ISSUE-04
@doc/execute.md#配置项#运行
>### 运行
>运行目标通过配置来持久化
>运行目标不能保存为全局，如果遇到保存全局运行命令，则报错【运行目标不能保存为全局】！
>运行目标需要以配置的形式持久化。
>./.darkhorse/code/run.toml里保存运行目标
>配置项举例（标准运行时）
>```
>darkhorse.code.run.target0.cmd="C:\Users\yujia\PycharmProjects\rag\.venv\Scripts\python.exe C:\Users\yujia\PycharmProjects\rag\src\P05-es\pdf2es.py"
>darkhorse.code.run.target0.name="pdf转储es"
>darkhorse.code.run.target0.bind=src\P05-es\pdf2es.py
>```
>target0 是用户随意取的名字，不是系统递增的（IDE不维护计数器）。
>只有IDE自动创建运行目标时，才遍历target开头的code/key，然后取最大的数字+1。

# ISSUE-05
>## 窗口
当开启多个darkhorse-code进程时，在Windows系统的窗口预览（Alt+Tab）里，这两个窗口的标题必须显示为项目名，若未打开项目则显示Darkhorse Code。

# ISSUE-06
创建文件后，自动将[navigator/project file]折叠了。请修改这种UI交互不友好的问题。

# ISSUE-07
直接modelScope下载，huggingface根本连不上，代码简单点

