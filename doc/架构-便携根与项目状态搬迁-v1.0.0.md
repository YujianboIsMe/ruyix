# 架构：便携根与项目状态搬迁（v1.0.0）

**归属版本**：v1.0.0　**上游需求**：`doc/需求-便携形态与零残留-v1.0.0.md`
**一句话**：① 把"全局路径从哪儿来"收敛成**唯一解析点**（根 = exe 同目录）；
② 把**项目侧 7 类写入从用户仓库搬进那个根**（按项目 key 分桶）。
两个目标共用一个前提：**所有路径必须出自同一处代码**，否则"零残留"必然漏。

---

## 一、改造清单（全量，带行锚 —— 逐项勾掉才算完成）

见需求文档 §二 的两张表（全局 8 处 / 项目侧 7 类）。这里只强调分类纪律：

| 类别 | 数量 | 处置 |
|---|---|---|
| 全局侧 `home_dir()` 拼接 | 8 处（7 个文件） | **全部收敛**到 `Paths` 门面 |
| 项目侧 `.ruyix` 写入 | 7 类（9 处代码） | **全部改指**根内 `projects/<key>/`（配置与数据同一个桶） |
| 项目侧**读取**（项目级 tools/skills/mcp/a2a） | 4 处 | 同步改指（读与写必须同一处解析，否则"读了旧的、写了新的"） |
| 沙箱副本里的 `.ruyix` | `engine/workspace.rs:253` | **不动**（那是沙箱复制品，不是用户仓库） |

## 二、`Paths` 门面（宿主唯一的解析点）

```rust
/// 唯一解析点。构造一次（启动最早期），注入给各处 —— 含引擎（引擎是库，不许自己找 exe）。
pub struct Paths {
    root: PathBuf,   // = exe 所在目录（或 RUYIX_HOME）
}

impl Paths {
    pub fn discover(exe_dir: &Path) -> Paths;          // §三 的判定只在这里
    pub fn global_dir(&self) -> PathBuf;               // <root>/global   全局配置 + 全局数据
    pub fn projects_dir(&self)-> PathBuf;              // <root>/projects 项目状态桶（一个项目一个）
    pub fn plugins_dir(&self)-> PathBuf;               // <root>/plugins  全局插件

    /// 项目 key（§五）：把任意项目路径映射成文件夹名
    pub fn project_key(&self, project: &str) -> String;
    /// `<projects>/<key>`（配置与数据同一个桶）。
    /// **参数是项目路径，不是 key** —— key 由内部算，调用方没机会"把路径当 key 用"。
    pub fn project_dir(&self, project: &str) -> PathBuf;
    /// 项目桶内的子目录（stage / backups / proc / env / sessions / verify / plugins）
    pub fn project_bucket(&self, project: &str, bucket: &str) -> PathBuf;

    // 全局数据（都在 global/ 下）
    pub fn runs_root(&self)   -> PathBuf;              // <global>/runs
    pub fn debug_log(&self)   -> PathBuf;              // <global>/logs/debug.log
    pub fn webview_data(&self)-> PathBuf;              // <global>/webview
    pub fn cache_dir(&self)   -> PathBuf;              // <global>/cache
}
```

**顶层只有三个目录，按"归属"分而不是按"配置/数据"分**：`global/`（大家的）、`projects/<key>/`（某个项目的）、
`plugins/`（插件的）—— 用户拍板的原话就是"一个可执行文件 + global + projects + plugins"；
少一层嵌套 = 少一层要解释的东西。全局配置与全局数据同住 `global/`，项目配置与项目数据同住一个桶，
**读写同一个函数**（避免"读了旧的、写了新的"）。

**没有"模式"分支**（第一版设计里的 Portable/Legacy 双根已取消）：根只有一个，
遗留目录**已经不存在**（2026-09-24 人工清完，见需求 §六）—— 代码里连"旧路径"这一支都没有，
架构因此比第一版更简单，这是用户澄清带来的红利。

**引擎侧**：不 `discover`。它从 `AppConfig.workspace_root` 拿根（字段已存在，`config_bridge.rs:69` 已在注），本单只改：
① 默认值去掉 `~/.darkhorse/harness/runs`（`engine/config.rs:41`）；② 相对路径解析相对**注入的根**而非 home（`:965-974`）；
③ **新增**：引擎需要知道"项目数据根"，这样 `stage`/`backups`/`proc` 才写得对 —— 用一个显式字段注入
**P1 的落地比初稿好一处**：不在 `AppConfig` 挂一个"根"字段，而是**宿主把项目状态目录算好注入**
（`<root>/projects/<key>`，逐项目）—— 免得 key 规则在引擎里再复制一份。P1 已先把引擎**不该有的能力**摘掉：
默认运行目录不再指向用户主目录（旧值 `~/.darkhorse/harness/runs` 已删）、`dirs` 依赖从引擎整个拿掉、
`~/` 的展开只剩 `expand_home` 一处。

## 三、根判定（唯一实现）

```
discover(exe_dir):
  1) RUYIX_HOME 非空 → root = RUYIX_HOME
  2) 否则            → root = exe 所在目录
```
**边界行为（都必须有明确动作，不许"看情况"）**：

| 情形 | 行为 |
|---|---|
| 根不可写（Program Files / 只读介质） | ✅ **已拍板**：原生 `MessageBoxW`【绿色软件无法安装在系统级目录】+ **拒绝启动**（退出码非 0，stderr 同打一份）；**不静默写到别处**。
（实测依据：只读根下 WebView2 直接 `os error 5` panic —— 没有"降级可跑"这条路，见需求 §八） |
| 根在系统临时目录（从 zip 里直接双击，Windows 解压到 `%TEMP%`） | ✅ **已拍板**：原生 `MessageBoxW`【必须解压后试用】+ **拒绝启动**（原"提醒但不阻断"作废）。
逃生口：`RUYIX_HOME` 指向固定位置时不判定为临时根（那是用户显式选的家） |
| 根在同一台机器被多个 Windows 用户使用 | 文档写明"便携版是单用户形态"；写失败走上一条 |
| `current_exe()` 失败 | 用 `RUYIX_HOME`；都没有则报错退出（不猜） |

判定只做一次并缓存；`--debug` 会话头**必须**打印 `根=… | 可写=… | webview=…`（A2/R13 的证据）。

**两个边界判定的实现口径**（P2）：

1. **可写性用"试写"探**（根里建一个临时文件再删），不读权限位 —— Windows 目录的
   "只读"属性对文件无约束力，而 Program Files 那种拒绝是 **ACL**（实测 `PermissionError`）；
   试写一次同时覆盖"ACL 不可写"与"只读介质"两种情况。
2. **弹框时机在 `tauri::Builder` 之前**：实测 builder 里失败是 panic（`app.rs:1425`），
   优雅不了；而 release 版是 windows 子系统、没有控制台 ⇒ 不弹框就等于"双击了没反应"。
   弹框走原生 `MessageBoxW`（此时还没有任何 webview 可以承载 HTML 文案）。
3. **临时根判定**：`RUYIX_HOME` 未设 **且** 解析出的根落在 `%TEMP%` / `%TMP%` 之下 ⇒ 拒。
   用户显式设了 `RUYIX_HOME` 就照用（哪怕那个位置在临时目录里）—— 判定只认"exe 同目录"这一种来源。

## 四、布局映射（旧 → 新，逐项机械可对）

| 内容 | 旧位置 | 新位置 |
|---|---|---|
| 全局分节配置 | `~/.ruyix/code/<section>.toml` | `<root>/global/<section>.toml` |
| 项目列表（每条带 `key`） | `~/.ruyix/code/projects.toml` | `<root>/global/projects.toml` |
| MCP / A2A / 工具 / 技能（全局） | `~/.ruyix/code/{mcp,a2a,tools,skills}.toml` | `<root>/global/…` |
| **项目作用域配置**（含项目级 mcp/a2a/tools/skills） | `<项目>/.ruyix/code/*.toml` | `<root>/projects/<key>/*.toml` |
| agent 沙箱产物 | `~/.ruyix/code/agent/runs` | `<root>/global/runs` |
| **确认模式暂存** | `<项目>/.ruyix/stage/**` | `<root>/projects/<key>/stage/**` |
| **写前备份** | `<项目>/.ruyix/backups/**` | `<root>/projects/<key>/backups/**` |
| **托管进程日志** | `<项目>/.ruyix/proc/*.log` | `<root>/projects/<key>/proc/*.log` |
| **环境安装留痕** | `<项目>/.ruyix/env/installs.jsonl` | `<root>/projects/<key>/env/installs.jsonl` |
| **会话存档** | `<项目>/.ruyix/code/agent/sessions/*.json` | `<root>/projects/<key>/sessions/*.json` |
| **验证产物** | `<项目>/.ruyix/target-verify`、`out-verify` | `<root>/projects/<key>/verify/{target,out}`（**绝对路径**注入） |
| 调试日志 | `~/.ruyix/code/debug.log` | `<root>/global/logs/debug.log` |
| WebView2 user data | `%LOCALAPPDATA%`（OS 默认） | `<root>/global/webview` |
| 插件（全局） | （无） | `<root>/plugins/` |
| 插件（项目专属） | `<项目>/.ruyix/plugins/` 不存在 | `<root>/projects/<key>/plugins/` |
| 命令发现缓存 | （进程内） | `<root>/global/cache/` |

> 用户仓库里**一个字节都不写**（`api/apply` 的产物写回除外 —— 那是用户要的结果，不是 IDE 的状态）。

## 五、项目 key 与索引

- **key 规则**（✅ 已拍板"可读 slug"；**P1 已实现 + 表驱动单测**）：
  1. 先 `canonicalize`（拿到磁盘上的真实大小写，顺带消掉 `..` 与短路径）；路径不存在就按原样算；
  2. 分隔符 → 单个 `-`，**盘符冒号丢掉**：`D:\Projects\Rust\ruyix` → `D-Projects-Rust-ruyix`；
  3. **Unicode 字母数字保留**（中文项目名不能全变 `_` —— 那会让"项目/我的项目"撞成同一个 key）；
  4. 非法的 Windows 字符（`<>:"/\|?*`）与控制字符 → `_`；首尾 `-`/`.` 裁掉；
  5. 超过 64 字符 → 前 48 字符 + `-<8 位 hash>`；**Windows 保留名**（`con`/`aux`/`nul`/`com1`…）
     追加同样后缀（否则那个目录在资源管理器里根本打不开）；
- **索引**：**不另开索引文件** —— `key` 直接写进 `<root>/global/projects.toml` 的项目条目里
  （与原路径同源，少一个要同步的文件；写入时机 = 打开项目时），
  同时写一份 `projects/<key>/project.toml` 自证，哪个文件夹属于哪个项目一眼可见；
- **孤儿**（✅ 已拍板"看得见 + 能删"）：项目被移动/删除后 `projects/<key>/` 仍在 →
  面板列出全部桶（名称 / 体积 / "不对应任何已知项目"标记），可**确认后整桶删**；**绝不自动删**；
  路径比较沿用现有 `clean_path` 纪律（大小写/短路径不猜）；
- **同名不同路径**永不撞（slug 不同）；**同路径不同副本**必然同 key（这正是"同一项目状态共享"的期待行为）。

## 六、搬迁的七处技术细节（照做，别各自发挥）

1. **`config.rs:911`（项目配置根）**：改为 `Paths::project_config(&key)`；读路径与写路径必须同一个函数（避免"读了旧的写了新的"）；
2. **`stage.rs:24` / `engine/agent.rs:2451`（暂存）**、**`apply.rs:25` / `stage.rs:126` / `engine/agent.rs:2413`（备份）**：
   改指 `project_bucket(key, "stage"|"backups")`。**路径封闭规则要跟着改**：现在拒绝写 `.ruyix/**`
   （`stage.rs:30,103`），改为**拒绝写 IDE 状态根**（`<root>/**`）—— 判据从"名字"换成"是不是我们的家"；
3. **`engine/proc.rs:211`（进程日志）**：改指 `project_bucket(key, "proc")`；日志是引擎写、按 pid 命名，路径由引擎从注入的状态根拼；
4. **`env_setup.rs:241`**：改指 `project_bucket(key, "env")`；
5. **`sessions.rs:123`**：改指 `project_bucket(key, "sessions")`；`agent/mod.rs:547` 那句
   "会话跟项目走：没有项目就没有 `<root>/.ruyix` 可写"的注释与逻辑要一起改（现在没有项目就**可以**有状态根）；
6. **`verify.rs:495,528`**：`CARGO_TARGET_DIR` 与输出目录改**绝对路径**注入（`<root>/projects/<key>/verify/…`）。
   注意：exe 与项目跨盘时 cargo 不能硬链接 → 编译慢一点（可接受，文档说明）；
7. **提示词与文案**：`AGENT_SYSTEM` 里两处"已暂存到 `.ruyix/stage/`"（`engine/agent.rs:1465,1528`）与
   系统提示里的 `.ruyix` 说明（`:484`）必须改成新位置 —— **模型看到错路径会去 `read` 一个不存在的地方**，
   这类"提示词描述与实际不符"在这个仓库里已经被抓过两次。

## 七、迁移 —— 不做（2026-09-24 定）

用户是 ruyix 唯一使用者，且残留已人工清完（需求 §六），全局 `~/.ruyix/code` **本来就是空的** ⇒
**不写迁移代码、不留幂等标记、不做旧目录扫描**：`Paths` 只有新根一种形态，代码里没有"旧路径"分支。

（需要搬家就手工拷 `*.toml` 进 `global/`；`projects/` 里那几桶整个拷走即为"带走项目状态"。）

## 八、WebView2 data dir 与单实例（**P0 已实测**，2026-09-24）

**结论一：WebView2 的用户数据目录能落进根内，机制是 `WebviewWindowBuilder::data_directory()`。**

- API（`tauri 2.11.5`）：`tauri::WebviewWindowBuilder::data_directory(PathBuf)`
  （`webview_window.rs:1024`；`WebviewBuilder` 上同名，`webview/mod.rs:963`）→ 透传给 wry 的
  `WebContext::new(Some(path))` → Windows 上就是 WebView2 的 **userDataFolder**。
  **不需要退路方案**（`WEBVIEW2_USER_DATA_FOLDER` 环境变量留在兜底位置，不必启用）。
- 实测（`node scripts/p0-probes.mjs`，两条臂都要跑）：

  | 臂 | 关键原始证据 | 判定 |
  |---|---|---|
  | 指定臂 `--data-dir <tmp>` | `profile_created_at_target=true`、`profile_path=<tmp>\EBWebView`、`new_localappdata_entries=[]`、`localappdata_identifier_touched=false`（`Local State` 时间戳前后**逐位相同**：`134347328037788554`） | ✅ 三个条件全达 |
  | 对照臂 `--default`（今日形态） | `localappdata_identifier_touched=true`（`...037788554` → `…456053498`）、`profile_path=%LOCALAPPDATA%\com.ruyix.code` | ✅ 对照组成立 → **不指定必然外溢**，不是我猜的 |

  → **A5 可做**：把 data dir 指到 `<root>/global/webview` 就成立，"删文件夹即净"成立。
  → 顺带记一笔现状体量：`%LOCALAPPDATA%\com.ruyix.code\EBWebView` 实测 **41MB**（老血统那台
  `com.darkhorse.harness` 还留着 **34MB**，是另一个值得在 release 里提一句的残留）。

**结论二：单实例互斥体按根区分可行；今天的写死名字确实会撞。**

| 臂 | 结果 | 判定 |
|---|---|---|
| 同根并发两份 | 一个 `first_instance=true`、一个 `false`，同一个名字 `Local\ruyix-instance-<root 指纹>` | ✅ P2-A |
| 不同根并发（按根命名） | 两个都 `true`，名字 `…-d0ca4b45c56029cb` vs `…-873e7090c06d4766` | ✅ P2-B |
| 不同根并发（`--name-mode global` = 现状） | 第二个 `false` —— 撞了 | ✅ P2-C（对照臂：所以现状必须改） |

两条读代码得到的事实（与上面互证）：① 这个互斥体**不阻止启动**，只让第二个实例不自动打开
上次项目（前端读 `is_another_instance` 命令）；② 两份副本真正的冲突点是**共享 WebView2 profile**，
所以 **data dir 与 mutex 必须一起按根**（同 §八 开头那条"两条同批做"）。

**探针怎么重跑**（一条命令跑完所有臂，打印原始输出 + 按预注册判据判定）：

```bash
node scripts/p0-probes.mjs          # 内部先 cargo build -p ruyix --examples
```

预注册判据写在脚本头部与 `src-tauri/examples/{webview_data_probe,instance_mutex_probe}.rs` 里；
退出码 0 = 全部达预期（当前 **8/8**）。

> ⚠️ **踩坑记录**（示例二进制为什么"一个字都不输出"）：示例拿不到 tauri-build 给主程序嵌的
> 应用清单，而 tao/wry 会导入 comctl32 **v6 才有**的 `SetWindowSubclass` / `TaskDialogIndirect`；
> 没清单时加载器挑 system32 里的 v5，进程在 `main` 之前就死在 **`0xC0000139`**
> （STATUS_ENTRYPOINT_NOT_FOUND），stdout/stderr 全空 —— 看起来像"探针没干活"。
> 修法：`src-tauri/build.rs` 补 `cargo::rustc-link-arg-examples=/MANIFEST:EMBED`
> + `examples/probe.manifest`（**只影响 examples，不碰发布产物**）。
> 排查路径留存：PE 导入表对比（两个 exe 导入的系统 DLL 一样）→ 二进制里搜
> `Microsoft.Windows.Common-Controls` / `SetWindowSubclass` → 差异只剩清单。

**P4 已接上（2026-09-24 同批做完）**：

| 接在哪 | 实现 | 判据（实测） |
|---|---|---|
| WebView2 数据目录 | `build_main_window` 里 `.data_directory(paths::current().webview_data())` | `portable-root-probe.mjs` 臂①：`%LOCALAPPDATA%\com.ruyix.code\EBWebView\Local State` mtime **逐位不变**、profile 落 `<根>/global/webview/EBWebView` |
| 单实例互斥体 | 名字 = `Local\ruyix-instance-<根指纹>`（`paths::root_fingerprint`，16 位 hex） | 臂⑤：两份副本**两个进程同时在**、各自建自己的 `global/` |

两条**同批**做完（P0 的结论：只做一半会得到"缓存被两份副本共用、第二个实例却起不来"的更坏状态）。
互斥体名字抽成纯函数 `mutex_name(fingerprint)` 是为了能单测 —— 名字写错的表现是"两份副本互相
以为对方是自己"，那要装了两份副本才会发现。

**A3/A4「仓库干净」怎么跑才可信**：两个版本，一个给门禁、一个给真机。

- **机器版**（每次 `cargo test` 都跑）：`src-tauri/src/agent/repo_clean_tests.rs` ——
  真造一个临时项目 + 临时便携根，真跑一遍 stage→apply→sessions→env→proc→verify
  （verify 会真跑 `cargo check`），然后断言：项目里新增的文件**恰好只有那一次写回**、
  没有 `.ruyix`/`target/`/`verify/`，而 state 全在 `<根>/projects/<key>/` 里。
  它当场抓到一处真残留（`Cargo.lock`）—— 这正是它存在的理由：残留不报错，只会静默多一个目录。
- **真机版**：`scripts/repo-clean-scenario.mjs`

- 快照写在**项目之外**（`%TEMP%/ruyix-repo-clean-scenario`）—— 一个用来证明"零写入"的工具，
  自己先不能往仓库里写；
- `--phase before` → （打开项目 → agent 暂存 → 确认写回 → 起托管服务 → 跑验证 → 收尾）→ `--phase after`
  → 自动判定：`git status --porcelain` 的**新增行**必须为空、`find -name .ruyix` 必须为空；
- `--selftest`：自检判定逻辑（"什么都没写"要放行、"写了 `.ruyix`"要报红）——收尾已跑通。

## 九、与插件化（v0.14）的接口

`<root>/plugins/`（全局）+ `<项目>/.ruyix/plugins/` 不再存在（**项目级插件放 `<root>/projects/<key>/plugins/`**，
与"用户仓库零写入"一致）——这条要与 v0.14 文档同步修订。

## 十、不变量与门禁

1. **路径只许在 `src-tauri/src/paths.rs` 里拼** —— 已成单测
   `paths::tests::no_global_path_construction_outside_this_file`：扫两棵源码树，`dirs::` 与 `.darkhorse`
   只许出现在 `paths.rs`（**注释里提历史不算**，只查代码）；引擎侧连 `dirs` 依赖都已摘掉，
   `~/` 的展开只剩 `expand_home`（读 `USERPROFILE`/`HOME`，不给引擎"自己找系统位置"的能力）；
2. **同一张门禁表还带 P3 棘轮**：`.ruyix` 的存量按文件记数（`P3_DEBT`），**只许减不许增**；
   按设计要永久保留的两处单列 `BY_DESIGN`（清沙箱复制品里的 `.ruyix`、知识库扫描跳过 `.ruyix`）；
3. **用户项目目录里不出现 IDE 文件** → 这是本单最有价值的判据，两层：
   - **动态**（主判据）：一个真机场景跑一遍（打开项目 → 让 agent 暂存一次 + 交付一次 → 起一个托管进程 → 跑一次验证），
     然后 `git status --porcelain` 只剩用户自己的改动、`find <项目> -name .ruyix` 为空；
   - **静态**（辅助）：全仓 grep `<项目>/.ruyix` 形态的拼接（防止有人新写一处）；
4. `Paths::discover` 表驱动单测：`RUYIX_HOME` × exe 同级 ×（有/无预置目录）× 可写性；
5. `project_key` 单测：Windows 路径、UNC、中文、超长、大小写；
6. 打包脚本用 `RUYIX_HOME` 指向临时目录（门禁不污染开发机）；
7. **便携根真机判据**：`node scripts/portable-root-probe.mjs`（A1/A2/A5/A11/A12 五条臂，
   跑真实 exe：空目录自建布局、`%LOCALAPPDATA%` 不动、临时根与只读 ACL 根各拒一次、
   原生弹框以窗口标题为证、两份副本并存）——当前 **17/17**；
8. **打包门禁**：`node scripts/package-portable.js` 自带解压自校验（sha256 逐项 + 目录齐全），
   "命令没报错"不算过。

## 十一、风险表

| 风险 | 影响 | 应对 |
|---|---|---|
| ~~Tauri v2 指定 WebView2 data dir 的 API 细节未测~~ | — | ✅ **已实测（P0）**：`WebviewWindowBuilder::data_directory` 可用且生效（§八 带原始证据）；退路 `WEBVIEW2_USER_DATA_FOLDER` 不需要启用 |
| 项目状态搬出后**换机不再带工作区状态** | 用户预期落差 | 文档明说"状态属于 IDE，不属于项目"；用户确需带走时可整体拷 ruyix 文件夹 |
| 项目移动 → key 变化 → 状态"孤儿" | 看起来像丢数据 | `global/projects.toml` 里的 key + 面板里"清理孤儿"；文档给"改回原路径即可找回"的说明 |
| ~~跨卷 verify 产物~~ | — | ✅ **已实测并拍板（P3 期实测 + 用户拍板）**：源码在 D:、target 在 C: 与同卷对照，逐轮 0.61/0.60/0.61 vs 0.63/0.72/0.62（中位数 **0.61 → 0.63，+3.3%**，预注册判据 >30% 才算显著）；两臂 rlib `nlink` 都是 **2** —— cargo 的硬链接只发生在 **target 目录内部**，跨卷与它无关（`os.link` 跨卷确实 EXDEV，但 cargo 不需要）。结论：**接受，不加开关、不加提示** |
| 路径长度（`<root>/projects/<key>/verify/…` 深目录） | Windows 260 限制 | key 截断 + hash；必要时启用长路径（文档说明） |
| ~~`.gitignore:21 /.ruyix` 留着的语义混淆~~ | — | ✅ **已消除**：该行 2026-09-24 已删（用户手动删） |
| 验证会跑用户自己的工具链，**工具链自己也往项目里写东西** | 用户 `git status` 里多出 `Cargo.lock` / `__pycache__` | **已处理两处**（被真跑的判据抓到）：`Cargo.lock` 验证前不存在就由我们删掉（`tidy_cargo_lock`）、`__pycache__` 用 `PYTHONPYCACHEPREFIX` 改道；其余工具链缓存保持工具的标准行为并写进 release 的"已知问题"；要一个字节都不动就用沙箱模式（验证跑在副本上） |
| 跨卷目标卷**非 NTFS**（exFAT 移动盘 / 网络盘） | 未知 | **未实测**（本机两卷都是 NTFS）—— release 已知问题里明说，不假装测过 |
| `CLAUDE.md` 里仍有多处 0.x 口径的路径描述 | 换台机器照它找会找不到 | 该文件受保护（写入需用户批准）：本轮尝试写入被拦，**留在待办**；口径以本文档与需求文档为准 |

## 十二、与排期的关系

施工顺序、产物与判据见 `doc/排期-1.0.0.md`：§二/§三 → P1–P2；§四/§五/§六 → **P3（最大一块）**；
§八 → P4；§十 → 贯穿所有阶段的门禁。（§七 迁移：不做，无对应阶段。）
