# 排期：v1.0.0（便携根 + 项目状态搬迁 + 便携发行）

**上游**：`doc/需求-便携形态与零残留-v1.0.0.md`（R1–R14 / A1–A12）· `doc/架构-便携根与项目状态搬迁-v1.0.0.md`
**目标**：exe 同目录 = 唯一根；全局与项目状态全在根内；**用户仓库零写入**；
发行物 = **一个可执行文件 + `global/` + `projects/` + `plugins/`**（已拍板）；zip 只是推荐分发形态。
卸载 = 删文件夹。
**完成定义（DoD）**：A1–A12 全绿（没绿要进 release 的"已知问题"）+ `doc/release-v1.0.0.md`（中英双语）
+ tag `v1.0.0` + 分支 `1.0.0` 快照（沿用"主干 master 开发、版本分支只作快照"的惯例）。

> **旗舰判据**（这一版真正值钱的东西）：
> **A3/A4 —— 打开项目、跑完一轮 agent（暂存 + 交付 + 起服务 + 验证）之后，
> 用户仓库里没有 `.ruyix`，`git status` 只剩用户自己的改动。**
> 建议把它放在发布门槛的第一条：它一条就覆盖了"零残留"的大半。

---

## 一、阶段总览

| 阶段 | 内容 | 产物 | 估时 |
|---|---|---|---|
| **P0** | 探针：WebView2 data dir（Tauri v2 怎么设）+ 两份副本/mutex 行为 + 设计 A3/A4 的跑法 | 结论写回架构 §八/§十一（带实测输出） | 1 天 |
| **P1** | 路径收敛：`Paths` 门面 + 全局 8 处 + 引擎默认值/注入字段 + `project_key` | `src-tauri/src/paths.rs`、改造后的 7 个文件、表驱动单测、静态门禁 | 1.5–2 天 |
| **P2** | 便携根：目录模板 + 首启创建 + 边界（只读 / 临时目录）+ 日志打印"根/可写" | 目录模板、边界行为、`--debug` 证据行 | 1 天 |
| **P3** | **项目状态搬迁（最大一块）**：7 类写点改指根内 + 路径封闭规则改写 + verify 绝对路径 + 提示词/文案路径同步 | 搬迁后的代码、`projects/<key>/{*.toml,stage,backups,proc,env,sessions,verify}` | 2–2.5 天 |
| **P4** | WebView2 data dir 落根内 + 单实例 mutex 按根区分（**必须同批**） | 窗口创建前设 data dir；mutex 带根 hash | 1 天 |
| **P6** | 打包与元数据：`scripts/package-portable`、zip、`SHA256SUMS.txt`、随包 README/LICENSE、`ruyix`/`1.0.0` | zip 产物、打包脚本、元数据对齐 | 1 天 |
| **P7** | 验收与发布：A1–A12 逐条留证据 + release 文档 + tag/分支 | 验收记录、`doc/release-v1.0.0.md` | 0.5–1 天 |

**合计约 8–9.5 个工作日**（单人）。
**关键路径**：P0 → P1 → P2 → **P3** → P4 → P6 → P7；P6 的脚本骨架可在 P1 期间先搭。

> **原 P5（迁移）已取消**（2026-09-24）：用户是 ruyix 唯一使用者，项目侧残留已人工清完、全局遗留目录本来就是空的
> （需求 §六）—— 不写迁移代码。省一天，也少一个可能出错的分支。

---

## 二、逐阶段：做什么、判据、风险、回退

### P0 探针与判据设计（1 天）—— 唯一的"未知数闸门"　✅ **已完成（2026-09-24）**

**结论一句话**：WebView2 data dir 能指定（`WebviewWindowBuilder::data_directory`，实测生效）、
单实例 mutex 按根命名可行（实测同根一个/不同根两个）、A3/A4 的场景跑法已成脚本。
**零产品代码改动**：新增两个 example 探针 + 两个脚本；`build.rs` 只多了一条"给 examples 嵌清单"的
链接参数（见架构 §八 的踩坑记录）。原始证据、预注册判据与判定都在 `node scripts/p0-probes.mjs` 的输出里（8/8）。

| 项 | 内容 | 判据 |
|---|---|---|
| 探针 1 | Tauri v2 能否指定 WebView2 data directory？用哪个 API？指定后启动正常吗？ | 指定临时目录后启动成功，且 `%LOCALAPPDATA%` 下不新增 ruyix 数据 |
| 探针 2 | mutex 名字加根 hash 后，两份解压副本能否并存；data dir 与 mutex 是否耦合 | 两份副本各起一个、各自可用 |
| 设计 | A3/A4「仓库干净」怎么跑才可信（场景脚本：打开项目 → agent 暂存一次 + 交付一次 → 起托管进程 → 跑一次验证 → 收尾） | 场景写进 `scripts/`（可重复跑，输出 `git status --porcelain` 与 `find . -name .ruyix` 的原始结果） |
| 落点 | 探针结论写回架构文档 §八/§十一，**带命令与原始输出** | 文档里能看见"实测过" |
| 风险 | Tauri 本体不支持 → 退路 `WEBVIEW2_USER_DATA_FOLDER`（建窗口前设），或明确宣布"WebView2 缓存不入根"并在 release 写清 |

### P1 路径收敛（1.5–2 天）

- `paths.rs`：`Paths` 门面（架构 §二）+ `discover`（§三）+ `project_key`（§五）+ 表驱动单测；
- 替换全局 8 处（`main.rs:67`、`config.rs:236/845/857`、`mcp.rs:53/62`、`a2a.rs:51/60`、`capability.rs:27/36`、`agent/config_bridge.rs:69`）；
- 引擎侧：`config.rs:41` 默认值、`:965-974` 相对根、**新增 `project_state_root` 注入字段**（架构 §二末）；
- **判据**：单测全绿；静态门禁（`home_dir()` 只许 `paths.rs` / 引擎 `expand_home`）；全仓无 `.darkhorse`；现有门禁零回归；
- **回退**：这是纯收敛（行为等价），任一判据红就回退单文件，不许顺手改行为。

### P2 便携根与目录模板（1 天）

- 首启按模板建 `global/`（含 `logs/`、`cache/`、`webview/`）、`projects/`、`plugins/highlight/`，各带 README；
  `global/` 带最小可用模板（**只放注释与空壳，不放示例密钥**）；
- 边界按架构 §三 表实现（只读 → 拒绝启动 + 人话；临时目录 → 提醒但不阻断）；
- `--debug` 会话头打印 `根=… | 可写=… | webview=…`；
- **判据**：A1、A2、R13（两种边界摆放各跑一次）。

### P3 项目状态搬迁（2–2.5 天）—— **这一版最容易出事故的一段**

按架构 §六 的七条逐条做，**每条都配一个判据**：

| 改哪 | 判据 |
|---|---|
| ① 项目配置根（`config.rs:911`） | 在项目里改一次项目作用域配置 → 落 `projects/<key>/*.toml`；用户仓库无新文件 |
| ② 暂存 + 备份（`stage.rs`、`apply.rs`、`engine/agent.rs`） | 确认模式跑一轮 → 暂存在 `projects/<key>/stage/`；交付覆盖已有文件 → 备份在 `projects/<key>/backups/` |
| ③ 进程日志（`engine/proc.rs:211`） | 起一个托管进程 → 日志在 `projects/<key>/proc/`；服务面板仍能读尾 |
| ④ 环境留痕（`env_setup.rs:241`） | 触发一次安装动作 → `projects/<key>/env/installs.jsonl` 多一行 |
| ⑤ 会话存档（`sessions.rs:123`） | 新建会话 → `projects/<key>/sessions/*.json`；重开仍能载入 |
| ⑥ verify 绝对路径（`verify.rs:495,528`） | 跑一次验证 → 产物在 `projects/<key>/verify/`；跨盘也要能跑（实测一次） |
| ⑦ 提示词与文案（`engine/agent.rs:484,1465,1528`） | 全仓 grep `.ruyix/stage` 只剩历史/迁移语境；模型提示词里的路径与实际落点**逐字一致** |
| ⑧ 路径封闭规则（`stage.rs:30,103`） | 试图把产物写进 IDE 状态根 → 被拒（新单测；旧的"拒写 `.ruyix`"用例改成"拒写状态根"） |

- **回退**：P3 期间旧代码路径与新的可以短暂并存（开关或分支），**一旦 A3/A4 绿了就删掉旧路径**，不留两套；
- **风险**：最容易漏的是**读**路径（项目级 tools/skills/mcp/a2a 的读取），
  漏了会表现成"项目配置改了不生效 / 读了旧的"→ 所以每条都要"写一处、读一处"成对验证。

### P4 WebView2 data dir + 单实例（1 天，按 P0 结论）

- **判据**：A5（`%LOCALAPPDATA%` 不新增）、A11（两份副本并存各自正常）；
- **风险**：这两条必须同批 —— 只做一半会出现"第二个实例起不来、缓存却被共用"的更坏状态。

### ~~P5 迁移~~ —— 已取消（2026-09-24）

残留已人工清完、全局 `~/.ruyix/code` 是空的、用户是唯一使用者 ⇒ 不写迁移代码、不留幂等标记、不做清理入口。
判据 A10 随之改口径（"空根首启即可用 / 全仓无迁移代码"）。以后真要搬家：手工拷 `*.toml` 进 `global/`。

### P6 打包与元数据（1 天）

- `scripts/package-portable.js`（零依赖 Node，照 `check-style.js` 风格）：`cargo tauri build` → 收 exe → 放模板目录 → 写 `SHA256SUMS.txt` → 打 zip 到 `dist/`；用 `RUYIX_HOME` 指定中间目录；
- 元数据：`tauri.conf.json` 的 `productName → ruyix`、`version → 1.0.0`；两个 `Cargo.toml` 版本同步；`description`/`authors` 复核；
- **判据**：A9（一条命令出 zip、解压逐项齐全、sha256 可校验）、A8（属性页 `ruyix`/`1.0.0`）；
- **风险**：`productName` 改动会牵动 exe 文件名与窗口标题（有测试引用）→ 改完**跑全门禁**，别只看编译过。

### P7 验收与发布（0.5–1 天）

- 逐条跑 A1–A12 并留证据（命令 + 原始输出贴进验收记录）；没绿的进 release 的"已知问题"；
- `doc/release-v1.0.0.md`：中英双栏，照 `release-v0.0.5.md` 的格式，**写清"状态都在程序目录里、怎么整体备份/搬迁"**（没有迁移步骤了）；
- 更新 `README.md` 的下载章节（zip 布局 + 纯 exe 也能跑 + 卸载 = 删文件夹）；
- tag `v1.0.0` + 分支 `1.0.0`（快照）。

---

## 三、这一版明确不做

- 不做安装器（NSIS/MSI）、不做自动更新、不做多用户/服务化（便携版是单用户形态，文档写明）；
- **不实现插件加载**（只建 `plugins/`，加载见 `doc/v0.x/需求-高亮插件化-v0.14.md`）；
- 不做"长期双根"，也**不做迁移代码**（残留已人工清完、全局遗留目录本来就是空的，见需求 §六）；
- 不改 agent 沙箱语义（`runs_root` 位置变，产物格式与隔离纪律不变）；
- 不自动删用户的任何东西（旧目录、孤儿状态都要显式同意）。

## 四、与其它单子的关系

| 单子 | 关系 |
|---|---|
| `需求-高亮插件化-v0.14.md` | 需要 `<root>/plugins/` 先落地 → **本单 P2 之后可并行开工**；其"项目级插件"路径要改成 `<root>/projects/<key>/plugins/`（与"仓库零写入"一致），那份文档已同步修订 |
| 宽行只读视图（v0.13，已交付） | 无依赖 |
| 会话 / 引擎产物 | P3 改位置，格式与语义不变；`doc/` 与 `CLAUDE.md` 里所有 `.ruyix` 路径描述要同步（R14，算在 P3 里） |

## 五、每阶段的统一门禁

```bash
cargo test && cargo clippy --all-targets && cargo fmt --check
node scripts/check-style.js && node scripts/ui-smoke.js
# 涉及前端/几何时另跑：node scripts/editor-wide-line.js（宽行）、node scripts/editor-layout.js（常规布局）
# P3 起每次都要跑：仓库干净场景（A3/A4）
```
（跑前 `export TMP=TEMP='C:\Users\yujia\AppData\Local\Temp'`。
**不推没验证过的东西**：每阶段判据真跑出输出再往下走。）

## 六、开工前需要你拍的板

**已拍板（2026-09-24）**：① 发行形态 = **一个可执行文件 + `global/` + `projects/` + `plugins/`**
（P2 的目录模板、P1 的 `Paths` 门面、P3 的搬迁落点都按这个实现；原 `config/` + `data/` 两分作废）；
② **不做迁移**（原 P5 取消，需求 §六）；③ 项目侧残留已人工清完（`D:\Projects` 两次级内 3 处全删，
存档 `D:\ruyix-legacy-20260924`）。

还剩需求文档 §七 这几条：项目 key 命名 / 只读目录行为 / 临时目录行为 /
跨卷 verify / 我们自己的 `.gitignore` 那行。
**这几条一确认，P0 立刻可开工（P0 零代码，只做探针 + 设计 A3/A4 的跑法）。**
