# Generated by Cargo
# will have compiled files and executables
/target/

# Remove Cargo.lock from gitignore if creating an executable, leave it for libraries
# More information here https://doc.rust-lang.org/cargo/guide/cargo-toml-vs-cargo-lock.html
Cargo.lock

# These are backup files generated by rustfmt
**/*.rs.bk

# 打补丁 / 合并时留下的原始副本，不是源码
*.orig
*.rej


# Added by cargo

/target
# 纯净模式（--no-default-features）的独立构建目录，见 doc/highlight-plugins.md
/target-pure/
/.idea

# 本地微调环境（WSL 脚本、日志与产物）
training/.wsl/
training/outputs/

# 便携包产物（scripts/package-portable.js 出的 zip 与组包目录）
/dist/

# macOS
.DS_Store
.workbuddy/

# 探针出的截图是 scratch（不该进仓库；我自己踩过一次：add -A 在前、移走文件在后）
/*-shot.png
/mem-probe.png
